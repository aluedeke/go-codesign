# go-codesign Environment Variables
#
# Copy this file to .env and update with your actual credentials
# Then source it before running go-codesign:
#   source .env
#   go-codesign resign --app=MyApp.ipa

# Path to P12 certificate file
CODESIGN_P12=/path/to/certificate.p12

# Path to provisioning profile (.mobileprovision)
CODESIGN_PROFILE=/path/to/profile.mobileprovision

# Password for the P12 certificate
CODESIGN_PASSWORD=your-password-here
